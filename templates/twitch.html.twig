<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Twitch - toham</title>
        <meta name="twitter:card" content="summary_large_image">
        <meta name="twitter:site" content="@boileau_thomas">
        <meta name="twitter:title" content="Twitch @toham">
        <meta name="twitter:description" content="Planning {{ planning }}">
        <meta name="twitter:image" content="{{ absolute_url(asset('uploads/' ~ planning.image)) }}">
        <meta name="twitter:creator" content="@boileau_thomas">
        <style>
            body {
                height: 100vh;
                width: 100vw;
                display: flex;
                justify-content: center;
                align-items: center;
                margin: 0;
                padding: 0;
                background: #122a59;
            }

            a {
                width: 100%;
                height: 100%;
                display: flex;
                justify-content: center;
                align-items: center;
            }

            img {
                width: 100%;
                max-height: 100%;
                aspect-ratio: 16/9;
            }
        </style>
    </head>
    <body>
        <a href="https://twitch.tv/toham">
            <img src="{{ absolute_url(asset('uploads/' ~ planning.image)) }}" alt="Planning {{ planning }}">
        </a>
    </body>
</html>